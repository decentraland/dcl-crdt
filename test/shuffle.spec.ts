import { crdtProtocol, Message, State } from '../src'
import expect from 'expect'

describe('Shuffle messages and get the same result', () => {
  it('should store the message A in all the clients', async () => {
  })
})
